<?php
$task = $this->task ?? null;
$isEdit = $task !== null;
?>

<body class="bg-gray-200">
    <div class="min-h-screen flex items-center justify-center">
        <div class="bg-gray-50 max-w-4xl w-[30%] min-h-[70vh] rounded-2xl shadow-[10px_10px_0_0_#000] p-8">

                <!-- Header -->
             <div class="flex justify-end mb-6 font-['Holtwood_One_SC',_serif] text-xl">
                <div class="flex items-center gap-2">

                <!-- Texto en dos líneas -->
                <div class="text-right leading-none">
                    <div class="font-['Holtwood_One_SC',_serif] text-2xl">
                        <?php echo $isEdit ? 'EDIT' : 'NEW'; ?>
                    </div>
                <div class="font-['Holtwood_One_SC',_serif] text-2xl">
                        TASK
                    </div>
                </div>

                <!-- Exclamación -->
                <div class="font-['Holtwood_One_SC',_serif] text-6xl leading-none">
                    !
                </div>

                </div>
            </div>

                <!-- Title -->
            <div class="form-group flex flex-col gap-1 w-full">
                <label for="title" class="font-['Holtwood_One_SC',_serif] text-xl">Title</label>

            <input
                type="text"
                id="title"
                name="title"
                required
                placeholder="Task title"
                value="<?php echo $isEdit ? htmlspecialchars($task['title']) : ''; ?>"
                class="w-full px-3 py-2 border border-neutral-800 rounded-lg
               font-['Baloo_2',_serif] focus:outline-none focus:ring-1 focus:ring-neutral-800 bg-gray-50">
            </div>

                <!-- Description -->
            <div class="form-group flex flex-col gap-1 w-full">
            <label for="description" class="font-['Holtwood_One_SC',_serif] text-xl mt-5">Description</label>

            <textarea
                id="description"
                name="description"
                placeholder="Optional description"
                rows="3"
                class="w-full px-3 py-2 border border-neutral-800 rounded-lg
                font-['Baloo_2',_serif] focus:outline-none focus:ring-1 focus:ring-neutral-800 bg-gray-50"
                ><?php echo $isEdit ? htmlspecialchars($task['description']) : ''; ?></textarea>
            </div>

<!-- Dates -->
<div class="grid grid-cols-2 gap-6 mt-4">

    <!-- Creation date -->
    <div class="flex flex-col items-start">
        <label for="created_at"
               class="font-['Holtwood_One_SC',_serif] text-sm text-right flex items-center gap-1">
               <img width="22" height="22"
                src="https://img.icons8.com/badges/50/calendar.png" alt="calendar"/>
            Creation date
        </label>

        <input
            type="date"
            id="created_at"
            name="created_at"
            required
            value="<?php echo $isEdit ? $task['created_at'] : ''; ?>"
            <?php echo $isEdit ? 'readonly' : ''; ?>
            class="mt-1 w-full px-3 py-2 border border-neutral-800 rounded-lg
                   font-['Baloo_2',_serif] bg-gray-50">
    </div>

    <!-- Due date -->
    <div class="flex flex-col items-start">
        <label for="due_date"
               class="font-['Holtwood_One_SC',_serif] text-sm text-left">
            End date
        </label>

        <input
            type="date"
            id="due_date"
            name="due_date"
            value="<?php echo $isEdit ? $task['due_date'] : ''; ?>"
            class="mt-1 w-full px-3 py-2 border border-neutral-800 rounded-lg
                   font-['Baloo_2',_serif] bg-gray-50"
        >
    </div>

</div>

    <!-- Actions -->
    <div class="form-actions">
        <button type="submit">
            <?php echo $isEdit ? 'Update task' : 'Save task'; ?>
        </button>

        <a href="/task">
            Go back
        </a>
    </div>

</form>

        </div>
    </div>
    

</body>