<?php
$users = $this->users ?? [];
$total = count($users);
$currentUser = $this->currentUser ?? null;
?>

<div class="min-h-screen flex items-center justify-center bg-gray-200 py-12 px-4 sm:px-6 lg:px-8">
    
    <!-- Card contenedor -->
    <div class="max-w-md w-full bg-white rounded-3xl p-8 border border-2 border-solid border-black" style="box-shadow: 8px 8px 0px 0px rgba(0, 0, 0, 1);">
        
        <!-- Título -->
        <div class="text-center mb-8">
            <h2 class="text-3xl font-black uppercase tracking-tight font-['Holtwood_One_SC',_serif]">
                Select User
            </h2>
        </div>

        <!-- Lista de usuarios -->
        <div class="space-y-4 mb-6">
            <?php if (!empty($users)): ?>
                <?php foreach ($users as $user): ?>
                    <div class="bg-gray-100 rounded-xl p-4 flex items-center justify-between hover:bg-gray-200 transition-colors">
                        
                        <!-- Avatar y username -->
                        <div class="flex items-center space-x-4">
                            <!-- Avatar -->
                            <div class="w-12 h-12 bg-black rounded-full flex items-center justify-center">
                                <svg class="w-7 h-7 text-white" fill="currentColor" viewBox="0 0 24 24">
                                    <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                                </svg>
                            </div>
                            
                            <!-- Username -->
                            <span class="text-lg font-medium text-gray-700">
                                <?php if (isset($user['username'])): ?>
                                    <?php echo htmlspecialchars($user['username']); ?>
                                <?php else: ?>
                                    <?php echo htmlspecialchars($user['name']); ?>
                                <?php endif; ?>
                            </span>
                        </div>

                        <!-- Botones de acción -->
                        <div class="flex items-center space-x-2">
                            <?php if ($this->canEdit): ?>
                                <!-- Cuando HAY sesión activa -->
                                
                                <!-- Botón ir a tareas (lápiz) -->
                                <a href="<?php echo WEB_ROOT; ?>/users/login-as?id=<?php echo $user['id']; ?>" 
                                   class="p-2 hover:bg-gray-100 rounded-lg transition-colors" 
                                   title="Go to your tasks">
                                    <svg class="w-5 h-5 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"/>
                                    </svg>
                                </a>
                                
                                <!-- Botón eliminar (X) -->
                                <a href="<?php echo WEB_ROOT; ?>/users/delete?id=<?php echo $user['id']; ?>"
                                    class="p-2 hover:bg-gray-100 rounded-lg transition-colors"
                                    title="<?php echo ($currentUser && $user['id'] == $currentUser['id']) ? 'Delete yourself - you will be logged out' : 'Delete user'; ?>"
                                    onclick="return confirm('<?php echo ($currentUser && $user['id'] == $currentUser['id']) ? 'Are you sure you want to delete yourself? You will be logged out.' : 'Are you sure you want to delete this user?'; ?>')">
                                    <svg class="w-5 h-5 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                                    </svg>
                                </a>
                            <?php else: ?>
                                <!-- Sin sesión - solo botón para ir a tareas (lápiz) -->
                                <a href="<?php echo WEB_ROOT; ?>/users/login-as?id=<?php echo $user['id']; ?>"
                                    class="p-2 hover:bg-gray-100 rounded-lg transition-colors"
                                    title="Go to your tasks">
                                    <svg class="w-5 h-5 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"/>
                                    </svg>
                                </a>
                            <?php endif; ?>
                        </div>
                    </div>
                <?php endforeach; ?>
            <?php else: ?>

                <!-- Estado vacío -->
                <div class="text-center py-8">
                    <div class="mb-4 flex items-center justify-center">
                        <img width="42" height="42" src="https://img.icons8.com/badges/96/user-folder.png" alt="user-folder"/>
                    </div>
                    <h3 class="text-xl font-bold text-gray-700 mb-2">No Users Yet</h3>
                </div>
            <?php endif; ?>
        </div>

        <!-- Botón Go back -->
        <div class="mt-6">
            <a href="<?php echo WEB_ROOT; ?>/" class="block w-full bg-black text-white font-['Holtwood_One_SC',_serif] py-3 px-4 rounded-lg hover:bg-gray-800 transition duration-200 text-center uppercase tracking-wide">
                Go back
            </a>
        </div>
    </div>
</div>
